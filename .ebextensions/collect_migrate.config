container_commands:
  00_docker_ps:
    command: docker ps -a
    leader_only: true
  01_compress_collect_migrate:
    command: "docker exec ${docker ps -l -q -f 'status=running'} /code/compress_collect_migrate.sh"
    leader_only: true
